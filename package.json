{
  "name": "sisar-2026",
  "version": "0.0.1",
  "private": true,
  "packageManager": "npm@11.5.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "dependencies": {
    "@nestjs/config": "^4.0.4"
  }
}
